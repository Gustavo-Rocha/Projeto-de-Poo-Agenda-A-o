
Vá até a classe GenericDao e  na propriedade "User", coloque  o usuário do seu banco de dados MySQL
na propriedade senha coloque a senha do seu banco de dados MySQL.


O USUÁRIO PARA LOGAR-SE NO SISTEMA É 
USUÁRIO: GROCHA
SENHA : 12345

Oformato da data é no padrão americano (yyyy/mm/dd)
o preenchimento da hora é no mesmo campo que a data 
exemplo:
2017/12/07 14:50



INSIRA OS COMANDOS ABAIXO no MySQL PARA CRIAR AS TEBELAS NO MySQL ASSIM COMO AS CHAVES PRIMÁRIAS 

CREATE DATABASE PROJETO_POO;

USE PROJETO_POO;

CREATE TABLE TB_CLIENTES (
	CPF VARCHAR(15) NOT NULL,
	NOME VARCHAR(45) NOT NULL,
	TELEFONE VARCHAR(15) NOT NULL,
	CELULAR VARCHAR(15) NOT NULL,
	EMAL VARCHAR(45) NOT NULL,
	CEP VARCHAR(10) NOT NULL,
	BAIRRO VARCHAR(45) NOT NULL,
	CIDADE VARCHAR(45) NOT NULL,
	UF CHAR(2) NOT NULL,
	OBSERVACAO VARCHAR(100)NOT NULL
);

-- DEFININDO A PRUMARY KEY

ALTER TABLE TB_CLIENTES ADD CONSTRAINT CPF PRIMARY KEY (CPF);



CREATE TABLE TB_LOGIN (
	USUARIO VARCHAR(15),
	SENHA VARCHAR (8)
	
);

INSERT INTO tb_login(USUARIO,SENHA) VALUES ('GROCHA',12345);

    
    
CREATE TABLE TB_AGENDAR (
	CPF VARCHAR(15) NOT NULL,
	DATAS VARCHAR(17) NOT NULL
    
);


ALTER TABLE TB_AGENDAR ADD CONSTRAINT DATAS PRIMARY KEY (DATAS);


